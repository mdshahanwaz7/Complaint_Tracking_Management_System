name: Build and Push MERN Images
# updatedd
on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Backend
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/mern-backend ./Backend
        docker push ${{ secrets.DOCKER_USERNAME }}/mern-backend

    # - name: Build and Push Frontend
    #   run: |
    #     docker build -t ${{ secrets.DOCKER_USERNAME }}/mern-frontend .
    #     docker push ${{ secrets.DOCKER_USERNAME }}/mern-frontend
